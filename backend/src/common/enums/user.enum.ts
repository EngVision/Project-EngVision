export enum Gender {
  Male = 'Male',
  Female = 'Female',
  Other = 'Other',
}

export enum Role {
  Student = 'Student',
  Teacher = 'Teacher',
  Admin = 'Admin',
}

export enum AccountStatus {
  Active = 'Active',
  Blocked = 'Blocked',
  Pending = 'Pending',
}
