export enum StatusCourseSearch {
  All = 'All',
  Attended = 'Attended',
  Published = 'Published',
  Draft = 'Draft',
}

export enum SortBy {
  time = 'time',
  price = 'price',
}

export enum Order {
  asc = 'asc',
  desc = 'desc',
}
