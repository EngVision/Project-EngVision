export enum StatusCourseSearch {
  All = 'All',
  Attended = 'Attended',
  Published = 'Published',
  Draft = 'Draft',
}

export enum SortBy {
  time = 'createdAt',
  price = 'price',
  level = 'level',
}

export enum Order {
  asc = 'asc',
  desc = 'desc',
}
