export enum StatusCourseSearch {
  All = 'All',
  Attended = 'Attended',
  Published = 'Published',
  Draft = 'Draft',
}

export enum SortBy {
  time = 'createdAt',
  price = 'price',
  level = 'level',
  star = 'star',
}

export enum Order {
  asc = 'asc',
  desc = 'desc',
}
